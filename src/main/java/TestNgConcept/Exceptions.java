package TestNgConcept;

public class Exceptions {

	/*
	 * 
	 * IllegalArgumentException : occurs when we are trying to send argument in code with the help of sendkeys and 
	 *    its blank then we get this error
	 *    
	 * NullPointerException : if we created object of any class and not storing value inside the created object we get this exception
	 * 				//if we pass incorrect data through config.properties file
	 * 
	 * driver.switchto().alert() ; //we are not assigning this value to alt object and if we use method alt.accept it will give error
	 * 
	 *   
	 *   org.openqa.selenium.ElementNotInteractableException
	 *   org.openqa.selenium.NoSuchFrameException:
	 */
}
