package TestNgConcept;

public class Locaters {

/*	//Absolute Xpath= it starts with forward backslash now a days nobody using it as any change get made in a code all root nodes
 * gets changed
 * Relative Xpath= it can get write from the middle of HTML tags it starts with // backslash there are 4 type of relative xpaths
 * 
 * Using Attributes syntax : //html-tag[@Attribute="value of Attribute"]
 * 								//input[@id="userid"];
 * Using Text Function 
							://html-tag(text()='value');
								//label[text()='Email ID must not be blank'];
	Using containsText function
							://htmltag[contains(text(),'value')] no need to take full value
								//label[contains(text(),'Email')]
	using contains attribute
							:htmltag[contains(@attribute,'value of attribute')]
							//label[contains(@class,'uid')];
							input[contains(@href,'link')];
							for multiple result try to do indexing for particular search
							
	if the xpath value is dynamic in nature use below syntax
							// tagname[starts-with(@attribute,'beginning value')]
							 input[starts-with(@id,'a-b-01')]
							 
	if you want to highlight all same elements which are available on DOM 
							 //tagname[contains(text(),'make flex')]/child:li
							 (//tagname[contains(text(),'make flex')]/child:li)[1]
							 
							 
}*/
