package TestNgConcept;

public class Assersion {
	
	//testng supports hard and soft assertion in hard assertion test execution is stopped if assertion got fail
	//hard assert gets apply on main functionality of application
	//soft assertion will continue its execution even if test execution got fail but result will come in failed test
	//for soft assertion first we need to create object and at the end we need to assert.assertAll(); statement
	//in this all soft assert created object can give one result with above method

}
